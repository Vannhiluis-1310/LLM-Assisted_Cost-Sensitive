---
phase: 3.2
status: passed-with-caveat
verified: 2026-05-29
---

# Phase 3.2 Execution Verification

## Verdict

PASS for source implementation and local `sample_100k` flow verification.

Caveat: the local run used `tfidf_svd_fallback`, not MiniLM, so it verifies the pipeline but does not support a final LLM-assisted performance claim. The notebook must be rerun on Colab or another environment where `sentence-transformers/all-MiniLM-L6-v2` is available.

## Checks

| Check | Status | Evidence |
|---|---|---|
| Notebook exists | PASS | `notebooks/05_llm_hybrid_policy.ipynb` |
| Notebook JSON valid | PASS | Parsed 28 cells successfully |
| Code cells parse | PASS | All code cells passed Python AST parse |
| Required inputs loaded | PASS | Phase 2 scores and Phase 3.1b tuned outputs loaded for `sample_100k` |
| Raw split alignment | PASS | Validation/test raw rows aligned to score rows by `TransactionID` |
| Text leakage audit | PASS | `results/phase32_text_leakage_audit_sample_100k.csv`, all splits passed |
| Embedding cache/alignment | PASS | `results/phase32_similarity_features_sample_100k.csv` has validation/test rows |
| Train-only prototypes | PASS | Fraud/legit prototypes computed from train embeddings only |
| Validation-only tuning | PASS | Gamma, hybrid score weight, and thresholds selected on validation only |
| Test report-only | PASS | Test rows generated only after candidate selection |
| Level 4 comparator consistency | PASS | `gamma=0.00` Level 5 matched tuned Level 4 after fixing bin assignment to match Phase 3.1b |
| Level 5 outputs | PASS | `phase32_*_sample_100k` files and figures created |
| Claim guard | PASS | Metadata blocks final LLM claim unless `embedding_backend_used = minilm` |

## Local Result Snapshot

| Cost | Local Backend | Selected Candidate | Test Total Cost | Delta vs Tuned Level 4 |
|---|---|---|---:|---:|
| Cost-A | `tfidf_svd_fallback` | `level5_embedding_similarity_threshold_adjustment`, gamma 0.00 | 20,332.934777 | 0.0 |
| Cost-B | `tfidf_svd_fallback` | `level5_embedding_similarity_threshold_adjustment`, gamma 0.00 | 40,665.869555 | 0.0 |
| Cost-C | `tfidf_svd_fallback` | `level5_embedding_similarity_threshold_adjustment`, gamma 0.00 | 99,834.099649 | 0.0 |

## Requirement Coverage

| Requirement | Status |
|---|---|
| LLM-05 | Implemented with MiniLM path plus fallback label; final claim requires MiniLM rerun |
| POL-07 | Implemented LightGBM score + embedding similarity candidates |
| POL-08 | Compared Level 5 directly against tuned Level 4 on same `RUN_MODE` |
| POL-09 | Claim guard implemented; local fallback result is reported honestly |
| LEAK-06 | Level 5 text leakage audit passed |

## Residual Risk

- Current local result is a fallback embedding run, not MiniLM.
- Level 5 did not improve over tuned Level 4 in local fallback mode.
- Disagreement cases are empty in the fallback-selected policy because validation selected `gamma = 0.00`; Colab MiniLM may produce non-zero adjustments.

## Next Action

Run notebook 05 on Colab with MiniLM enabled, then use the downloaded `results/phase32_*` files for Phase 4 final analysis.
